name: Gerar APK

on:
  push:
  workflow_dispatch:

jobs:
  android:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Localizar projeto
        shell: bash
        run: |
          f=$(find . -name capacitor.config.json -not -path "*/node_modules/*" | head -1)
          if [ -z "$f" ]; then echo "::error::capacitor.config.json não encontrado. Suba os arquivos do zip no repositório."; exit 1; fi
          echo "PROJ=$(dirname "$f")" >> "$GITHUB_ENV"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
      - run: npm install --no-audit --no-fund
        working-directory: ${{ env.PROJ }}
      - run: npx cap add android
        working-directory: ${{ env.PROJ }}
      - run: npx cap sync android
        working-directory: ${{ env.PROJ }}
      - run: chmod +x android/gradlew && cd android && ./gradlew assembleDebug
        working-directory: ${{ env.PROJ }}
      - uses: actions/upload-artifact@v4
        with:
          name: diario-sintomas-apk
          path: ${{ env.PROJ }}/android/app/build/outputs/apk/debug/app-debug.apk
